rootProject.name = "kotlin-mod-utils"

pluginManagement {
    repositories {
        mavenLocal()

        maven(url = "https://maven.msrandom.net/repository/root")

        gradlePluginPortal()
    }
}
